<form method="post" action="{{ path('app_order_customer_delete', {'id': order_customer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_customer.id) }}">
    <button class="btn btn-outline-danger mt-1">Suprimer</button>
</form>
